package pollsus;

public class PollStatistics {
    
    public int printAnswers(answers) {
        try {

            for (int first=0; first < answers.length; first = first+1 ) {
                System.out.println(polls[first][0]);
            }
        } catch (Exception e) {
            // TODO ~
        } 
        return 1; 
    }
}
